name: ai_camera
description: "AI Camera App by Chandresh"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+5
  flutter_tts: ^3.8.5
  shared_preferences: ^2.2.2
  google_generative_ai: ^0.4.0
  speech_to_text: ^6.6.0

flutter:
  uses-material-design: true
